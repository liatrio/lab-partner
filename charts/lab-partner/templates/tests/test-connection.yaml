apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab-partner.fullname" . }}-test-connection"
  labels:
    {{- include "lab-partner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab-partner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
